// import { fromJS } from 'immutable';
// import { makeSelectMessageInputDomain } from '../selectors';

// const selector = makeSelectMessageInputDomain();

describe('makeSelectMessageInputDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
