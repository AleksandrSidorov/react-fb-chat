import styled from 'styled-components'

const HeaderInfoActivity = styled.span`
  color: rgba(0, 0, 0, .4);
  font-size: 11px;
  font-weight: normal;
  margin-top: 2px;
  vertical-align: middle;
  white-space: nowrap;
  display: block;
`

export default HeaderInfoActivity
