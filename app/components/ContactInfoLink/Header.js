import styled from 'styled-components'

const Header = styled.h4`
  padding: 0;
  margin: 0;
  margin-bottom: 10px;
  font-weight: normal;
  font-size: 14px;
  color: rgba(0, 0, 0, 0.4);
`

export default Header
